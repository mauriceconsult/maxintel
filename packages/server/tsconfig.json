{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "types": ["bun", "node"] // ← must be explicit — overrides don't merge
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"]
}

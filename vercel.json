{
  "framework":      null,
  "installCommand": "bun install",
  "buildCommand":   "bun run build:packages && bunx prisma generate",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 300
    }
  }
}
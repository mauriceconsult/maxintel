{
  "name": "maxintel-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build:shared": "bun run --cwd packages/shared build && bun run --cwd packages/database build",

    "dev:server": "bun run build:shared && bun run --hot packages/server/src/index.ts",

    "dev:cli": "bun run build:shared && bun run --watch packages/cli/src/index.tsx"
  },
  "devDependencies": {
    "@eslint/js":          "^10.0.1",
    "@types/bun":          "^1.3.14",
    "@types/node":         "^20.19.43",
    "eslint":              "^10.6.0",
    "typescript":          "^6.0.3",
    "typescript-eslint":   "^8.63.0"
  },
  "dependencies": {
    "@ai-sdk/google": "^4.0.16",
    "hono":           "^4.12.29"
  }
}